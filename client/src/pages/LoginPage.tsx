export function LoginPage() {
  return (
    <section className="panel">
      <p className="eyebrow">Coming next</p>
      <h2>Auth UI will land here</h2>
      <p>
        This placeholder keeps the route structure stable while the backend auth
        slice is implemented test-first.
      </p>
    </section>
  );
}
